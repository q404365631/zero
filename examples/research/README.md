# Research Example

This example exercises the public research command chain against bundled paper
fixtures.

```bash
just research-example
```

It writes `artifacts/research-example/research.json` with hunt, edge,
convergence, thesis, score, meta, and sharpen reports. The artifact is
paper-only, local-only, and does not contain private venue material.
